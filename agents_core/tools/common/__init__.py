"""Reusable Tool implementations (Bitrix, PG, Telegram, RAG, HH) for all agents.

Each module exposes `TOOLS: list[Tool]` that callers register into their
ToolRegistry. Integration tests against real APIs live next to each module.
"""
