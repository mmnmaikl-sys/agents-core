"""Hello-world agent — end-of-Волна-0 reference.

When the library is complete, this script must stand up a 3-tool agent and
answer a simple question in ≤30 lines of application code. Stub now so the
file is present from day 1; fill it in at task 0.25.
"""

if __name__ == "__main__":
    raise SystemExit(
        "hello_agent.py is not implemented yet — will land in task 0.25 "
        "after the ReActLoop / ToolRegistry / LLMClient modules ship."
    )
